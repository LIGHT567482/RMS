{
  "name": "light-distributor-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron-main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . \"Light Distributor\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icons/icon.ico"
  },
  "devDependencies": {
    "electron": "^25.7.0",
    "electron-packager": "^17.1.1"
  }
}
